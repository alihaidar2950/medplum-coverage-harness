import type { LoopLog } from '../loop/iteration-logger.js';

/**
 * Render the human-readable loop summary from the iteration log
 * (reports/loop-<timestamp>.md). TODO.
 */
export function renderLoopReport(_log: LoopLog): string {
  throw new Error('TODO: implement loop report renderer');
}
